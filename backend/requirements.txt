# Core FastAPI
fastapi
uvicorn[standard] # Includes performance extras like watchfiles (for --reload)

# Pydantic (often used with FastAPI)
pydantic

# Environment Variables
python-dotenv

# Langchain Core & Integrations
langchain
langchain-community # Common integrations
langchain-ollama    # If using Ollama
langchain-groq      # If using Groq
tiktoken            # Often needed for token counting

# Vector Stores
weaviate-client     # For Weaviate Cloud
# faiss-cpu           # Uncomment if USE_LOCAL_VECTOR_STORE is True in config

# Document Processing
unstructured[pdf,docx] # Base + PDF and DOCX support

# Database
pymongo             # For MongoDB (chat history, metadata)

# Add any other specific dependencies your project uses here
